apiVersion: v1
kind: Service
metadata:
  name: tawazun-svc
  namespace: mshaikh-tawazun
  labels: 
    name: Mohammed Shaikh
spec:
  selector:
    app: deploy
  ports:
    - port: 80 
      targetPort: 8080
  type: LoadBalancer  